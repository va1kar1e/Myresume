import Profile from "@images/profile.jpg";

const ProfileImage = () => (
  <div className="ProfileImage">
    <img src={Profile} className="App-logo" alt="logo" height="100" />
  </div>
);

export default ProfileImage;
